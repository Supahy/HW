package TheLongestShortestWord;

public class WordLengthApp {
    public static void main(String[] args) {
        String[] sent = {"For", "given", "text", "find", "the", "longest", "and", "the", "shortest", "words"};
        int min = sent[0].length;
        for (int i = 0; i < sent.length; i++) {
            int number = Integer.parseInt(sent.length[i]);

        }





    }
}
